package se.kth.ockel.id1212.hw5.model;

/**
 * Enum for the different types of command that can be be parsed by the game logic.
 */
public enum GameCommand {
    MAKE_GUESS, FETCH_INFO, START_GAME, INVALID_COMMAND, EXIT, RESTART
}
